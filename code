import pandas as pd

# Define the paths to the extracted CSV files
startup_data_csv_path = os.path.join(extract_dir, 'startup data.csv')
data_csv_path = os.path.join(extract_dir, 'data.csv')
startup_csv_path = os.path.join(extract_dir, 'startup.csv')

# Read and display the first few rows of each file
print("\n--- First 5 rows of 'startup data.csv' ---")
try:
    df_startup_data = pd.read_csv(startup_data_csv_path)
    print(df_startup_data.head())
except FileNotFoundError:
    print(f"File not found: {startup_data_csv_path}")
except Exception as e:
    print(f"Error reading {startup_data_csv_path}: {e}")

print("\n--- First 5 rows of 'data.csv' ---")
try:
    df_data = pd.read_csv(data_csv_path, encoding='latin1') # Added encoding='latin1'
    print(df_data.head())
except FileNotFoundError:
    print(f"File not found: {data_csv_path}")
except Exception as e:
    print(f"Error reading {data_csv_path}: {e}")

print("\n--- First 5 rows of 'startup.csv' ---")
try:
    df_startup = pd.read_csv(startup_csv_path)
    print(df_startup.head())
except FileNotFoundError:
    print(f"File not found: {startup_csv_path}")
except Exception as e:
    print(f"Error reading {startup_csv_path}: {e}")
